server:
  port: ${CONFIG_PORT:8001}

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  application:
    name: config-server
